using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParedesInvisHades : MonoBehaviour
{
    public GameObject hadesWalls;
    // Start is called before the first frame update
    void Start()
    {
        hadesWalls.gameObject.SetActive(false);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
